# React-Jobs: My first project

# To run this project in your local
- Clone this repo
- Edit the `vite.config.js`, change the `port` to a port available on your computer, as well as the `target` to the route given to your by react in your pc.
- Run `npm run dev` and `npm run server` to start the application

